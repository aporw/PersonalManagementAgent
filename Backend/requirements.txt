fastapi
uvicorn
openai
python-dotenv
PyJWT
gunicorn
pytest
passlib[bcrypt]
scikit-learn
joblib

#fastapi → backend framework

#uvicorn → runs the FastAPI server

#openai → call the OpenAI / ChatGPT API

#python-dotenv → load API keys from .env file (very important)